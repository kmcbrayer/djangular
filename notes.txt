TODOs:
    pycharm javascript linting
    python templating stuff, write it?

models:
	python manage.py validate # validate models
settings:
	south is a bitch
	on app creation you need to:
		create database # according to database_name in settings.py
		run syncdb
		1 add tastypie to installed_apps
		2 run manage schemamigration <app_name> --initial

		do 1,2,3 for api
south migrations:
    after initial setup
    just do:
        manage.py migrate <app_name> #prob api
urls:

set it to where api is own app:
	write to models and urls and apis.py
	append urls to main urls.
