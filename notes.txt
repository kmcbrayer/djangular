django tastypie
models:
	python manage.py validate # validate models
settings:
	south is a bitch
	on app creation you need to:
		create database # according to database_name in settings.py
		run syncdb
		1 add tastypie to installed_apps
		2 run manage schemamigration <app_name> --initial
		3 run manage migrate --fake
		do 1,2,3 for backend

TODOs:
fix urls
add data in admin
get api working

string.replace:
infile = open('path/to/input/file')
outfile = open('path/to/input/file', 'w')

replacements = {'zero':'0', 'temp':'bob', 'garbage':'nothing'}

for line in infile:
    for src, target in replacements.iteritems():
        line = line.replace(src, target)
    outfile.write(line)
infile.close()
outfile.close()


